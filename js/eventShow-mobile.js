const gallerySlider = new Swiper('.gallerySlider', {
    speed:1000,
    loop: true,
  
   
    // Navigation arrows
    navigation: {
      nextEl: '.nextGallery',
      prevEl: '.prevGallery',
    },
  
  });